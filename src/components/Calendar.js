import React from 'react'
import Layout from "./Layout"

export default function Calendar() {

  return(
    <Layout>
      <h1
        style={{minHeight: `90vh`}}
      >Calendar Secret Content</h1>
    </Layout>
  )
}
